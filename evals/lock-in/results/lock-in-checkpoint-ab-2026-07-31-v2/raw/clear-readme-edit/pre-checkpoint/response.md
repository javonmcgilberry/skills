Draft brief: In root `README.md`, replace only `## Installing manually` with `## Manual installation`, preserving all other text and unrelated working-tree edits. Completion requires the new heading to exist, the old heading to be absent, and `git diff --check` to pass.

Approve this brief for execution with `$brrr`?